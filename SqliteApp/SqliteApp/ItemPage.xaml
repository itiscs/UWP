<Page
    x:Class="SqliteApp.ItemPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SqliteApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
                HorizontalAlignment="Center">
        <TextBlock x:Name="txtName" FontSize="30" Text="{Binding Name}" Margin="0,20,0,0"/>
        <TextBlock x:Name="txtDescription" FontSize="20" Text="{Binding Description}" Margin="0,20,0,0"/>
        <TextBlock x:Name="txtPrice" FontSize="25" Text="{Binding Price}" Margin="0,20,0,0"/>
        <Button x:Name="btnUpdate" Content="Update Tile" FontSize="20" Margin="0,20,0,0" Tapped="btnUpdate_Tapped"       />
        <Button x:Name="btnSecTile" Content="Secondary Tile" FontSize="20" Margin="0,20,0,0" Tapped="btnSecTile_Tapped"       />
    </StackPanel>
</Page>
