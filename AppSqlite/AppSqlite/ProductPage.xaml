<Page
    x:Class="AppSqlite.ProductPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AppSqlite"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Orientation="Vertical">
            <TextBox x:Name="txtName" FontSize="30" Text="{Binding Name,Mode=TwoWay}" Margin="0,20,0,0"/>
            <TextBox x:Name="txtDescription" FontSize="20" Text="{Binding Description,Mode=TwoWay}" Margin="0,20,0,0"/>
            <TextBox x:Name="txtPrice" FontSize="25" Text="{Binding Price, Mode=TwoWay}" Margin="0,20,0,0"/>
            <Button x:Name="btnSave" Content="Изменить" Click="btnSave_Click" FontSize="24"/>
        </StackPanel>

    </Grid>
</Page>
