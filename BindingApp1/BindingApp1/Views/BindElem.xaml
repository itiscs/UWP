<Page 
    x:Class="BindingApp1.BindElem"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BindingApp1"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel Margin="20" HorizontalAlignment="Center"  VerticalAlignment="Top" >
            <TextBlock Text="{Binding FirstName}" 
                                       Margin="15"/>
            <TextBlock Text="{Binding LastName}" 
                                       Margin="15"/>
            <TextBlock Text="{Binding Age,Mode=TwoWay}"  
                                       Margin="15"/>
            <Button Content="Change" FontSize="16" Click="Button_Click"/>
        </StackPanel>

    </Grid>
</Page>
